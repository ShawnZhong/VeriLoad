name: ci

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Checkout musl
        run: git submodule update --init --recursive third_party/musl

      - name: Install dependencies
        run: ./install_deps.sh

      - name: Build
        run: make compile tests musl

      - name: Run
        run: ./run.sh
